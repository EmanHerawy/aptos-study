module game_token::message {
    use std::error;
    use std::signer;
    use std::string;
    use aptos_framework::event;
    #[test_only]
    use std::debug;

    //:!:>resource
    struct TokenState has key {
        
    }
    struct TokenMetaData{
        
    }
    //<:!:resource



}
